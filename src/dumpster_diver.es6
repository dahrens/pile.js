"use strict";

import EventEmitter from 'events';


/**
 * Find stuff in one or more Buckets.
 */
export class DumpsterDiver extends EventEmitter {
  /**
   * Create a fresh and hungry dumpster diver.
   * Do not forget to place some interesting things in your Bucket! :)
   */
  constructor() {
    super();
  }
}
